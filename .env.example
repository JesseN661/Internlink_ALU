# InternLink – Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# Flask
SECRET_KEY=replace-with-a-long-random-string
FLASK_DEBUG=1

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=internlink
